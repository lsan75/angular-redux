/// <reference path="../typings/node/node.d.ts" />
var ts = require('typescript');
var path = require('path');
var os = require('os');
var CompilationRequest = (function () {
    function CompilationRequest(inputFileName, input, options) {
        this.input = input;
        this.inputFileName = path.normalize(inputFileName);
        this.options = options;
        if (options.newLine === 0 /* CarriageReturnLineFeed */) {
            this.newLine = '\r\n';
        }
        else if (options.newLine === 1 /* LineFeed */) {
            this.newLine = '\n';
        }
        else {
            this.newLine = os.EOL;
        }
    }
    CompilationRequest.prototype.isInputFile = function (fileName) {
        return path.normalize(fileName) === this.inputFileName;
    };
    return CompilationRequest;
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = CompilationRequest;
