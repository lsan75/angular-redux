var KEY = 'webpackTypeScript'.toUpperCase();
var enabled = new RegExp('\\b' + KEY + '\\b', 'i').test(process.env.NODE_DEBUG || '');
var indent = '';
var trace = enabled ? function (message) {
    console.error(KEY + ' ' + process.pid + ': ' + message.replace(/^/g, indent));
} : function () { };
trace.enabled = enabled;
trace.increaseIndent = function () {
    indent += '\t';
};
trace.decreaseIndent = function () {
    if (indent === '') {
        throw new Error("can't decrease zero indent");
    }
    indent = indent.slice(0, -1);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = trace;
